// These are part of the assignments a the end of JavaScript fundamentals in the Odin Project.

// Basic Arithmetic operations.

let sum = 5 + 6;
console.log(sum)

let sixFigures = 1 + 2 + 3 + 4 + 5 + 6;

console.log(sixFigures)

let basicOperation = (4 + 6 + 9) / 77;
console.log(basicOperation)


let a = 10;
console.log(9 * a)

let max = 57;

let actual = max - 13;

let percentage = actual / max;


//Unary && Binary Operators

let mangoes = "10";
let apples = "12";

alert( +mangoes + +apples ); //prepending the Unary Operator to each variable name converts the string to a number.

alert( mangoes + apples); // Binary operators concatenate strings so the result = mangoesapples.

//Increment Prefix && Postfix
let counter = 0;
counter++ // This outputs the previous value so it's = 0; postfix
++counter // This outputs a new number 1; prefix
